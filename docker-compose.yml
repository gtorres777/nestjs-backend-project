version: '3'

services: 
  nest-app:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    volumes:
      - .:/app
      - /app/node_modules

  mongo:
    image: "mongo"
    ports:
      - 27017:27017
